# 智慧企业运营管理平台部署与项目现状说明

> 文档更新时间：2026-09-11  
> 当前用途：老板验收与产品展示  
> 当前状态：已上线，生产环境运行正常

## 1. 当前部署信息

| 项目 | 当前配置 |
| --- | --- |
| 正式验收地址 | <https://smart-enterprise-operation-manageme.vercel.app/> |
| GitHub Pages 备用地址 | <https://caiyuqing1124.github.io/Smart-Enterprise-Operation-Management-Platform/> |
| 部署平台 | Vercel |
| 套餐 | Hobby 免费版 |
| Vercel 管理账号 | `caiyuqing1124@gmail.com` |
| Vercel 项目名 | `smart-enterprise-operation-management-platform` |
| GitHub 仓库 | <https://github.com/caiyuqing1124/Smart-Enterprise-Operation-Management-Platform> |
| 生产分支 | `main` |
| 应用框架 | Vue 3 + Vite |
| Framework Preset | Vite |
| 根目录 | `./` |
| 安装命令 | `npm ci` |
| 构建命令 | `npm run build` |
| 构建输出目录 | `dist` |
| Node.js 要求 | 20.19.0 或更高兼容版本 |
| 环境变量 | 当前不需要 |
| HTTPS | 由 Vercel 自动提供 |
| 自动部署 | 已开启，`main` 分支更新后自动发布到 Vercel 和 GitHub Pages |

Vercel 管理账号只记录登录邮箱，项目文件中没有保存 Google、GitHub 或 Vercel 的密码、验证码和访问令牌。

GitHub Pages 作为备用网址使用。由于项目部署在 GitHub 用户域名的仓库子目录中，Pages 构建会自动使用 `/Smart-Enterprise-Operation-Management-Platform/` 资源前缀和 Hash 路由；Vercel 构建仍使用根路径和 History 路由。

## 2. 当前线上验证结果

2026-09-11 已完成以下检查：

- Vercel 生产部署状态为 `Ready`。
- 正式地址可以正常打开登录页面。
- 页面标题、表单和静态资源加载正常。
- 直接访问 `/projects` 等业务地址时，会正确进入登录页面，不会出现服务器 404。
- 线上浏览器控制台没有发现错误或警告。
- 当前生产版本对应 GitHub `main` 分支的最新成功部署提交。
- 本地执行 `npm run build` 成功。

生产构建会提示 ECharts 公共包体积较大，但不影响当前验收使用。

## 3. 路由与部署配置

项目使用 Vue Router History 模式。仓库根目录的 `vercel.json` 已将所有访问路径回退到 `index.html`，用于保证以下场景正常工作：

- 直接打开项目、销售、财务等业务页面；
- 在业务页面刷新浏览器；
- 从外部链接进入系统内部路径。

当前配置：

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

## 4. 当前项目情况

本项目版本为 `1.0.0`，是一个纯前端企业运营管理平台，主要技术包括：

- Vue 3
- Vite
- JavaScript
- Element Plus
- Vue Router
- Pinia
- ECharts

当前已完成的业务范围：

- 统一工作台：经营工作台、我的工作台、消息中心；
- 经营管理：经营驾驶舱、经营目标、指标中心、经营分析报告；
- 客户与销售：客户、联系人、跟进、商机、合同与回款、销售分析；
- 项目交付：项目、任务、里程碑、风险、资源和项目文件记录；
- 财务运营：财务概览、收支流水、应收应付和结算登记；
- 平台设置：企业信息、组织、员工和基础资料维护；
- 账号入口：注册、登录、找回密码和退出登录。

已实现的主要业务联动：

- 登记合同回款后，同步更新合同、客户、财务台账、经营指标和消息；
- 修改项目任务进度后，重新计算项目总体进度；
- 客户验收通过后，联动合同状态和验收款计划；
- 项目风险和验收退回会生成风险消息；
- 当前周期经营数据会读取客户、项目和财务的最新业务数据。

## 5. 数据保存方式

本版本没有服务器和数据库，所有业务数据来自 `src/mock/` 中的前端 Mock 初始数据。

用户注册和使用系统后：

- 账号信息保存在当前浏览器的 Local Storage；
- 登录会话保存在 Local Storage 或 Session Storage；
- 业务数据保存在当前浏览器的 IndexedDB；
- 不同企业名称使用不同的本地业务数据空间。

这意味着：

- 同一浏览器刷新页面后，修改的数据仍然保留；
- 更换电脑、浏览器或浏览器用户后，数据不会自动同步；
- 使用无痕模式或清理浏览器数据后，本地账号和业务数据可能丢失；
- 修改 Mock 初始数据不会自动覆盖已经使用过的浏览器数据；
- 如需重新加载最新初始数据，可在账号信息中执行“恢复初始业务数据”。

## 6. 当前版本限制

- 不连接真实后端、数据库、短信、邮件或第三方审批服务；
- 账号与密码仅用于前端验收，不具备正式生产系统的安全能力；
- 找回密码验证码在前端生成，仅用于流程展示；
- 项目文件功能只保存文件名称、大小等记录，不会真正上传文件；
- 多个用户或多台电脑之间不能共享业务数据；
- 当前主要面向桌面端，不承诺手机端显示效果；
- 经营历史趋势包含固定 Mock 快照；
- 当前部署用于验收展示，不应描述为已具备真实企业生产运营能力。

## 7. 后续修改和发布流程

为避免老板正在验收时误更新正式站点，后续需求和 Bug 建议采用以下流程：

1. 从最新 `main` 创建独立开发分支，例如 `codex/fix-project-filter`；
2. 在本地修改需求或修复 Bug；
3. 执行 `npm run build`，确保生产构建通过；
4. 推送开发分支，使用 Vercel 自动生成的 Preview 地址检查；
5. 由需求方确认 Preview 页面；
6. 确认后合并到 `main`；
7. Vercel 和 GitHub Pages 自动更新各自的正式地址；
8. 再次检查两个正式地址和关键业务路径。

正式网址不会因为正常更新而改变。老板已收藏的地址可以继续使用，但已打开的页面可能需要刷新才能看到新版本。

## 8. 更新可能产生的影响

- 普通样式、文字和页面逻辑修改通常不会影响浏览器中已有业务数据；
- 如果修改 IndexedDB 数据结构、企业数据作用域或数据版本，必须增加兼容或迁移处理；
- 如果修改 Mock 初始数据，已经使用过的浏览器通常需要执行“恢复初始业务数据”才能看到新数据；
- 如果生产构建失败，应停止上线并修复，不能跳过构建检查；
- 如果新版本上线后发现严重问题，Vercel Hobby 可以回退到紧邻的上一个生产版本。

## 9. 免费网址说明

当前 Vercel 生产网址没有设置固定到期日期。只要账号、项目和生产域名未被删除，且使用情况符合 Vercel Hobby 套餐规则，网址可以继续使用。

需要注意：

- Hobby 套餐受免费额度和公平使用规则约束；
- 历史部署可能按 Vercel 的保留规则清理，但当前绑定正式域名的生产部署会继续保留；
- Hobby 套餐主要面向个人和非商业用途；
- 如果系统后续转为公司长期正式使用，建议升级合适的商业套餐或迁移到公司自己的服务器。

Vercel 参考文档：

- <https://vercel.com/docs/plans/hobby>
- <https://vercel.com/docs/git>
- <https://vercel.com/docs/deployments/rollback-production-deployment>

## 10. 老板验收前准备

建议在实际用于演示的电脑和浏览器中提前完成：

- 打开正式验收地址；
- 注册用于验收的企业账号；
- 检查企业名称和管理员姓名；
- 浏览工作台、客户、合同、项目和财务页面；
- 准备一条客户、合同、回款和项目验收的完整操作路径；
- 不使用无痕模式，不清理该浏览器的数据；
- 在老板所使用的网络环境下提前测试 Vercel 地址访问速度。

## 11. 本地维护命令

```bash
# 安装锁定版本的依赖
npm ci

# 启动本地开发环境
npm run dev

# 执行生产构建
npm run build

# 本地预览生产构建
npm run preview
```

每次准备上线前，至少需要保证 `npm run build` 成功，并人工检查受本次修改影响的页面。
