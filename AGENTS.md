# 智慧企业运营管理平台开发规则

## 项目性质

本项目仅为前端项目。

禁止添加：

- Java
- Python
- Node.js 后端
- MySQL
- PostgreSQL
- Redis
- 服务端 API

## 技术栈

- Vue 3
- Vite
- JavaScript
- Element Plus
- Vue Router
- Pinia
- ECharts

## 数据

所有业务数据统一使用前端 Mock 数据。

Mock 数据统一放在：

`src/mock/`

需要页面间共享的数据，不要重复定义。

## UI 要求

系统必须具有企业运营管理软件风格。

不同页面必须根据业务使用不同布局。

禁止所有页面统一采用：

“四张卡片 + 表格 + 一个图表”

## 页面质量

禁止出现：

- lorem ipsum
- 示例
- demo
- 模板
- 演示账号
- TODO 页面
- 空页面
- 空按钮

所有主要按钮必须有交互。

## 操作

### 新增

必须弹出表单。

### 编辑

必须可编辑。

### 删除

必须有确认。

### 查看

必须进入详情或打开详情。

### 筛选

必须实际改变数据。

## 开发规则

每次优先修改当前任务相关文件。

不得无原因重构整个项目。

不得删除已完成页面。

## 验证

每次开发完成后执行：

`npm run build`

如果失败，必须修复。

## 输出

每次任务结束时，用非程序员能理解的语言说明：

1. 本次完成了什么
2. 用户现在可以做什么
3. 我需要手工检查什么
4. 是否存在已知问题
5. build 是否通过
